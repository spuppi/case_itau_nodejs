version: "3.9"

services:
  api:
    build: .
    container_name: conta-corrente-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./prisma:/usr/src/app/prisma
    restart: unless-stopped
